

pub enum Command {
    Add,
    Remove,
    List,
}

#[derive(Debug)]
pub struct Book {
    pub name: String,
    pub author: String,
    pub date: String,
    pub id: i32
}

fn main() {
    // pass
}

